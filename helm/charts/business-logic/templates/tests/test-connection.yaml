apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "business-logic.fullname" . }}-test-connection"
  labels:
    {{- include "business-logic.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "business-logic.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
